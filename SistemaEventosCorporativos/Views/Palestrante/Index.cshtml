@model IEnumerable<SistemaEventosCorporativos.Models.Palestrante>

@{
    ViewData["Title"] = "Palestrantes";
}

<h1>Palestrantes</h1>

<p>
    <a asp-action="Create">Novo Palestrante</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Currículo</th>
            <th>Evento</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nome</td>
                <td>@item.Curriculo</td>
                <td>@item.Evento?.Nome</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>